# ======================================================================
# images.yaml
# Framely – Mega DevOps AKS Project
#
# Purpose:
# - Define Docker image names for each application
# - Keep image naming consistent across environments
# - Decouple image naming from build logic
#
# NOTE:
# - These are logical image names (not filesystem paths)
# - Registry URL is resolved separately via registries.yaml
# - Final image format:
#   <registry>/<repository>/<image>:<tag>
# ======================================================================

images:
  backend: framely/backend            # Backend API Docker image
  frontend-admin: framely/frontend-admin
                                     # Admin frontend Docker image
  frontend-customer: framely/frontend-customer
                                     # Customer frontend Docker image
